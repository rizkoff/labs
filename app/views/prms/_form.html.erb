<%= form_for(@prm) do |f| %>
  <% if @prm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@prm.errors.count, "error") %> prohibited this prm from being saved:</h2>

      <ul>
      <% @prm.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :pval %><br />
    <%= f.text_field :pval %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
